import { postList } from '../../../helpers/post-list';
import { module, test } from 'qunit';

module('Unit | Helper | post list');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = postList(42);
  assert.ok(result);
});
